(function () {
  const dataUrl = "assets/data/farm-talk-episodes.json";
  const eastern = "America/New_York";
  const liveState = document.getElementById("live-state");
  const liveEpisode = document.getElementById("live-episode");
  const streamPanel = document.getElementById("stream-panel");
  const countdown = document.getElementById("countdown");
  const archivePlayer = document.getElementById("archive-player");

  function parts(date) { return new Intl.DateTimeFormat("en-US", { timeZone: eastern, weekday: "long", month: "2-digit", day: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit", hourCycle: "h23" }).formatToParts(date).reduce(function (all, part) { all[part.type] = part.value; return all; }, {}); }
  function currentEastern() { return parts(new Date()); }
  function easternDateIso(date) { const p = parts(date); return p.year + "-" + p.month.padStart(2, "0") + "-" + p.day.padStart(2, "0"); }
  function episodeDateLabel(value) { return new Intl.DateTimeFormat("en-US", { timeZone: "UTC", month: "short", day: "numeric", year: "numeric" }).format(new Date(value + "T12:00:00Z")); }
  function nextWednesday() { const p = currentEastern(); const date = new Date(Date.UTC(Number(p.year), Number(p.month) - 1, Number(p.day), 12)); const days = (3 - date.getUTCDay() + 7) % 7; date.setUTCDate(date.getUTCDate() + days); if (days === 0 && Number(p.hour) >= 13) date.setUTCDate(date.getUTCDate() + 7); return date.toISOString().slice(0, 10); }
  function easternNoonUtc(dateString) { const fields = dateString.split("-").map(Number); const sample = new Date(Date.UTC(fields[0], fields[1] - 1, fields[2], 17)); const local = parts(sample); const offset = Date.UTC(Number(local.year), Number(local.month) - 1, Number(local.day), Number(local.hour), Number(local.minute)) - sample.getTime(); return new Date(Date.UTC(fields[0], fields[1] - 1, fields[2], 12) - offset); }
  function stateFor(episode, now) { const today = easternDateIso(now); const p = currentEastern(); const hour = Number(p.hour); if (episode.date > today) return "up-next"; if (episode.date === today && p.weekday === "Wednesday" && hour >= 12 && hour < 13) return "live"; return episode.audioUrl ? "listen-now" : "coming-soon"; }
  function escapeHtml(value) { const div = document.createElement("div"); div.textContent = value || ""; return div.innerHTML; }
  function topicMarkup(topics) { return topics && topics.length ? '<ul class="topic-list">' + topics.slice(0, 4).map(function (topic) { return "<li>" + escapeHtml(topic) + "</li>"; }).join("") + "</ul>" : ""; }
  function statusLabel(state) { return ({ "listen-now": "Listen now", "coming-soon": "Coming soon", "up-next": "Up next", "live": "Live now" })[state]; }
  function renderLive(data) {
    const now = new Date(); const nextDate = nextWednesday(); const episode = data.episodes.find(function (entry) { return entry.date === nextDate; }) || data.episodes.find(function (entry) { return stateFor(entry, now) === "live"; }); const hasLive = episode && stateFor(episode, now) === "live";
    liveState.textContent = hasLive ? "On air now" : "Next live broadcast";
    liveEpisode.innerHTML = episode ? '<h3>' + escapeHtml(episode.guest) + '</h3><p>' + escapeHtml(episode.title) + '</p><p>' + escapeHtml(episode.summary) + '</p>' + topicMarkup(episode.topics) : "<p>The next Farm Talk episode is being prepared.</p>";
    const streamUrl = data.streamFallbackUrl;
    if (hasLive) streamPanel.innerHTML = '<p class="stream-label">Live on WTBQ</p><audio controls preload="none" src="' + encodeURI(streamUrl) + '">Your browser does not support audio playback.</audio><a class="btn" href="' + streamUrl + '" target="_blank" rel="noopener noreferrer">Open WTBQ live stream</a>';
    else streamPanel.innerHTML = '<p class="stream-label">WTBQ live stream</p><div class="stream-player-muted"><audio controls preload="none" src="' + encodeURI(streamUrl) + '" aria-hidden="true" tabindex="-1">Your browser does not support audio playback.</audio></div><a class="btn" href="' + streamUrl + '" target="_blank" rel="noopener noreferrer">Open WTBQ live stream</a>';
    function updateCountdown() { if (hasLive) { countdown.textContent = "Farm Talk is on air now."; return; } const difference = Math.max(0, easternNoonUtc(nextDate).getTime() - Date.now()); const values = [Math.floor(difference / 86400000), Math.floor((difference % 86400000) / 3600000), Math.floor((difference % 3600000) / 60000), Math.floor((difference % 60000) / 1000)]; countdown.innerHTML = '<span class="countdown-value">' + values.map(function (value) { return String(value).padStart(2, "0"); }).join(":") + '</span><span class="countdown-units"><span>days</span><span>hours</span><span>mins</span><span>secs</span></span>'; }
    updateCountdown(); window.setInterval(updateCountdown, 1000);
  }
  function renderArchive(episodes, now) {
    const playable = episodes.filter(function (episode) { return Boolean(episode.audioUrl); }); const selected = playable[0] || episodes[0];
    function nowPlaying(episode) { const isPlayable = Boolean(episode.audioUrl); return '<div class="now-playing"><p class="eyebrow">' + (isPlayable ? "Now playing" : "Episode selected") + '</p><h3>' + escapeHtml(episode.guest) + '</h3><p class="episode-title">' + escapeHtml(episode.title) + '</p><p class="episode-summary">' + escapeHtml(episode.summary) + '</p>' + topicMarkup(episode.topics) + (isPlayable ? '<audio controls preload="metadata" src="' + encodeURI(episode.audioUrl) + '">Your browser does not support audio playback.</audio>' : '<p class="recording-note">This episode is listed now and will become playable as soon as its recording is published.</p>') + '</div>'; }
    function row(episode, index) { const state = stateFor(episode, now); const playable = Boolean(episode.audioUrl); return '<button class="queue-row' + (episode === selected ? " is-selected" : "") + '" type="button" data-index="' + index + '"' + (playable ? "" : " disabled") + '><span class="queue-index">' + String(index + 1).padStart(2, "0") + '</span><span class="queue-copy"><strong>' + escapeHtml(episode.guest) + '</strong><span>' + episodeDateLabel(episode.date) + '</span></span><span class="status status-' + state + '">' + statusLabel(state) + '</span></button>'; }
    archivePlayer.innerHTML = nowPlaying(selected) + '<div class="queue"><p class="queue-heading">All episodes</p>' + episodes.map(row).join("") + '</div>';
    archivePlayer.querySelectorAll(".queue-row").forEach(function (button) { button.addEventListener("click", function () { const episode = episodes[Number(button.dataset.index)]; archivePlayer.querySelector(".now-playing").outerHTML = nowPlaying(episode); archivePlayer.querySelectorAll(".queue-row").forEach(function (rowEl) { rowEl.classList.toggle("is-selected", rowEl === button); }); const player = archivePlayer.querySelector("audio"); if (player) player.play().catch(function () {}); }); });
  }
  fetch(dataUrl).then(function (response) { if (!response.ok) throw new Error("Episode data could not be loaded."); return response.json(); }).then(function (data) { const now = new Date(); const episodes = data.episodes.slice().sort(function (a, b) { return b.date.localeCompare(a.date); }); renderLive(data); renderArchive(episodes, now); }).catch(function () { archivePlayer.innerHTML = '<p class="loading">The episode archive is temporarily unavailable. Please visit <a href="https://wtbq.com/">WTBQ</a> to listen live.</p>'; liveState.textContent = "Farm Talk"; liveEpisode.innerHTML = "<p>Wednesdays, 12:00–1:00 p.m. Eastern.</p>"; streamPanel.innerHTML = '<a class="btn" href="https://wtbq.com/" target="_blank" rel="noopener noreferrer">Visit WTBQ</a>'; });
}());
